(** Artifacts.

  Artifacts are objects that can directly be obtained
  through the API to expose various results of ImandraX's work. *)

module Artifact = Artifact
module Proto = Proto

(** {2 Re-export} *)

module Artmsg = Artmsg
